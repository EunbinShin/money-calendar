import React from 'react';
import Calendar from 'react-calendar'

const MainCalendar = () => {
    return (
        <div>
            <Calendar />
        </div>
    );
};

export default MainCalendar;